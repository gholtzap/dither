[package]
name = "dither"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "dither"
path = "src/main.rs"

[dependencies]
dither-core = { path = "../dither-core" }
dither-io = { path = "../dither-io" }
eframe = "0.35.0"
rfd = "0.17.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
